import React from 'react';
import './Header.css'; // Import the CSS file for styling

const Header = () => {
  return (
    <div className="header-container">
      <h1 className="header-title">New Post</h1>
    </div>
  );
};

export default Header;
